name: tagentra_pm3
description: iOS BLE and local TCP transport for Tagentra PM3 Core.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.13.2
  flutter: '>=3.35.0'
dependencies:
  flutter:
    sdk: flutter
flutter:
  plugin:
    platforms:
      ios:
        pluginClass: TagentraPm3Plugin
